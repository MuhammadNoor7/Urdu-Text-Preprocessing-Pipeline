# Urdu-Text-Preprocessing-Pipeline
An NLP pipeline for preprocessing Urdu text and generating headlines from cleaned content. The system performs normalization, noise removal, token filtering, and metadata extraction, converting raw text into structured outputs. Implemented in Python with Jupyter Notebook, producing cleaned text, JSON metadata, and processed datasets for analysis.
